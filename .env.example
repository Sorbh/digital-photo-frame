# Digital Photo Frame Environment Configuration

# Server Configuration
# Internal container port (usually keep as 3000)
CONTAINER_PORT=3000
# External host port (change this to use different port on your machine)
HOST_PORT=3000
NODE_ENV=production

# Session Security
# Generate a secure session secret (e.g., using: openssl rand -base64 32)
SESSION_SECRET=your-secure-session-secret-change-this

# Admin Authentication
# Default admin password is 'admin123' - CHANGE THIS FOR SECURITY!
# Use plain text for development, or bcrypt hash for production
ADMIN_PASSWORD=your-secure-password

# Google Photos Integration (Optional)
# Get these from Google Cloud Console: https://console.cloud.google.com/
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback